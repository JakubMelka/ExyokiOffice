// Copyright (c) 2026 Jakub Melka and Collaborators
// SPDX-License-Identifier: MIT
// See LICENSE file in the project root for full license text.
// -----------------------------------------------------------------------------
// <auto-generated>
//     This file was generated by ExyokiOffice OpenXML generator.
//     Manual changes to this file may cause unexpected behaviour.
// </auto-generated>
// -----------------------------------------------------------------------------

#include "doctest.h"

#include "DomTestSupport.hpp"

#include "ExyokiOffice/DOM/DocumentFormat/OpenXml/Office2013/WebExtentionPane.hpp"
#include "ExyokiOffice/StandardTypes.hpp"

#include <cstdint>
#include <string_view>
#include <vector>

namespace EoNs = ExyokiOffice::DocumentFormat::OpenXml::Office2013::WebExtentionPane;

TEST_SUITE("DocumentFormat.OpenXml.Office2013.WebExtentionPane.DomGeneratedTests")
{
    TEST_CASE("WebExtensionTaskpane typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::WebExtensionTaskpane>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetDockState(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetDockState().IsDefined());
        hosted.element->SetVisibility(ExyokiOffice::BooleanValue{});
        CHECK_FALSE(hosted.element->GetVisibility().IsDefined());
        hosted.element->SetWidth(ExyokiOffice::DoubleValue{});
        CHECK_FALSE(hosted.element->GetWidth().IsDefined());
        hosted.element->SetRow(ExyokiOffice::UInt32Value{});
        CHECK_FALSE(hosted.element->GetRow().IsDefined());
        hosted.element->SetLocked(ExyokiOffice::BooleanValue{});
        CHECK_FALSE(hosted.element->GetLocked().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::StringValue sampleDockState = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetDockState(sampleDockState);
        CHECK(hosted.element->GetDockState() == sampleDockState);
        const ExyokiOffice::BooleanValue sampleVisibility = ExyokiOffice::BooleanValue(true);
        hosted.element->SetVisibility(sampleVisibility);
        CHECK(hosted.element->GetVisibility() == sampleVisibility);
        const ExyokiOffice::DoubleValue sampleWidth = ExyokiOffice::DoubleValue(1.5);
        hosted.element->SetWidth(sampleWidth);
        CHECK(hosted.element->GetWidth() == sampleWidth);
        const ExyokiOffice::UInt32Value sampleRow = ExyokiOffice::UInt32Value(static_cast<ExyokiOffice::UInt32>(1));
        hosted.element->SetRow(sampleRow);
        CHECK(hosted.element->GetRow() == sampleRow);
        const ExyokiOffice::BooleanValue sampleLocked = ExyokiOffice::BooleanValue(true);
        hosted.element->SetLocked(sampleLocked);
        CHECK(hosted.element->GetLocked() == sampleLocked);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetDockState() == sampleDockState);
        CHECK(reloaded.element->GetVisibility() == sampleVisibility);
        CHECK(reloaded.element->GetWidth() == sampleWidth);
        CHECK(reloaded.element->GetRow() == sampleRow);
        CHECK(reloaded.element->GetLocked() == sampleLocked);
    }

    TEST_CASE("OfficeArtExtensionList typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::OfficeArtExtensionList>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("WebExtensionPartReference typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::WebExtensionPartReference>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetId(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetId().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::StringValue sampleId = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetId(sampleId);
        CHECK(hosted.element->GetId() == sampleId);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetId() == sampleId);
    }

    TEST_CASE("Taskpanes typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::Taskpanes>();
        REQUIRE(hosted.element != nullptr);
    }

}
