// Copyright (c) 2026 Jakub Melka and Contributors
// SPDX-License-Identifier: MIT
// See LICENSE file in the project root for full license text.
//
// Derived from the Open-XML-SDK metadata under data/, which is
// Copyright (c) .NET Foundation and Contributors and MIT-licensed.
// See data/LICENSE and THIRD-PARTY-LICENSES.md in the project root.
// -----------------------------------------------------------------------------
// <auto-generated>
//     This file was generated by ExyokiOffice OpenXML generator.
//     Manual changes to this file may cause unexpected behaviour.
// </auto-generated>
// -----------------------------------------------------------------------------

#include "doctest.h"

#include "DomTestSupport.hpp"

#include "ExyokiOffice/DOM/DocumentFormat/OpenXml/Office2010/PowerPoint.hpp"
#include "ExyokiOffice/StandardTypes.hpp"

#include <cstdint>
#include <string_view>
#include <vector>

namespace EoNs = ExyokiOffice::DocumentFormat::OpenXml::Office2010::PowerPoint;

TEST_SUITE("DocumentFormat.OpenXml.Office2010.PowerPoint.DomGeneratedTests")
{
    TEST_CASE("NullEventRecord typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::NullEventRecord>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetTime(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetTime().IsDefined());
        hosted.element->SetObjectId(ExyokiOffice::UInt32Value{});
        CHECK_FALSE(hosted.element->GetObjectId().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::StringValue sampleTime = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetTime(sampleTime);
        CHECK(hosted.element->GetTime() == sampleTime);
        const ExyokiOffice::UInt32Value sampleObjectId = ExyokiOffice::UInt32Value(static_cast<ExyokiOffice::UInt32>(1));
        hosted.element->SetObjectId(sampleObjectId);
        CHECK(hosted.element->GetObjectId() == sampleObjectId);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetTime() == sampleTime);
        CHECK(reloaded.element->GetObjectId() == sampleObjectId);
    }

    TEST_CASE("SeekEventRecord typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::SeekEventRecord>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetTime(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetTime().IsDefined());
        hosted.element->SetObjectId(ExyokiOffice::UInt32Value{});
        CHECK_FALSE(hosted.element->GetObjectId().IsDefined());
        hosted.element->SetSeek(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetSeek().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::StringValue sampleTime = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetTime(sampleTime);
        CHECK(hosted.element->GetTime() == sampleTime);
        const ExyokiOffice::UInt32Value sampleObjectId = ExyokiOffice::UInt32Value(static_cast<ExyokiOffice::UInt32>(1));
        hosted.element->SetObjectId(sampleObjectId);
        CHECK(hosted.element->GetObjectId() == sampleObjectId);
        const ExyokiOffice::StringValue sampleSeek = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetSeek(sampleSeek);
        CHECK(hosted.element->GetSeek() == sampleSeek);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetTime() == sampleTime);
        CHECK(reloaded.element->GetObjectId() == sampleObjectId);
        CHECK(reloaded.element->GetSeek() == sampleSeek);
    }

    TEST_CASE("ResumeEventRecord typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::ResumeEventRecord>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("PauseEventRecord typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::PauseEventRecord>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("StopEventRecord typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::StopEventRecord>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("PlayEventRecord typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::PlayEventRecord>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("TriggerEventRecord typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::TriggerEventRecord>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetType(ExyokiOffice::EnumValue<ExyokiOffice::DocumentFormat::OpenXml::Presentation::TriggerEventValues>{});
        CHECK_FALSE(hosted.element->GetType().IsDefined());
        hosted.element->SetTime(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetTime().IsDefined());
        hosted.element->SetObjectId(ExyokiOffice::UInt32Value{});
        CHECK_FALSE(hosted.element->GetObjectId().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::EnumValue<ExyokiOffice::DocumentFormat::OpenXml::Presentation::TriggerEventValues> sampleType = ExyokiOffice::EnumValue<ExyokiOffice::DocumentFormat::OpenXml::Presentation::TriggerEventValues>(ExyokiOffice::DocumentFormat::OpenXml::Presentation::TriggerEventValues::none);
        hosted.element->SetType(sampleType);
        CHECK(hosted.element->GetType() == sampleType);
        const ExyokiOffice::StringValue sampleTime = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetTime(sampleTime);
        CHECK(hosted.element->GetTime() == sampleTime);
        const ExyokiOffice::UInt32Value sampleObjectId = ExyokiOffice::UInt32Value(static_cast<ExyokiOffice::UInt32>(1));
        hosted.element->SetObjectId(sampleObjectId);
        CHECK(hosted.element->GetObjectId() == sampleObjectId);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetType() == sampleType);
        CHECK(reloaded.element->GetTime() == sampleTime);
        CHECK(reloaded.element->GetObjectId() == sampleObjectId);
    }

    TEST_CASE("TracePointList typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::TracePointList>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("TracePoint typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::TracePoint>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetTime(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetTime().IsDefined());
        hosted.element->SetXCoordinate(ExyokiOffice::Int64Value{});
        CHECK_FALSE(hosted.element->GetXCoordinate().IsDefined());
        hosted.element->SetYCoordinate(ExyokiOffice::Int64Value{});
        CHECK_FALSE(hosted.element->GetYCoordinate().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::StringValue sampleTime = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetTime(sampleTime);
        CHECK(hosted.element->GetTime() == sampleTime);
        const ExyokiOffice::Int64Value sampleXCoordinate = ExyokiOffice::Int64Value(static_cast<ExyokiOffice::Int64>(1));
        hosted.element->SetXCoordinate(sampleXCoordinate);
        CHECK(hosted.element->GetXCoordinate() == sampleXCoordinate);
        const ExyokiOffice::Int64Value sampleYCoordinate = ExyokiOffice::Int64Value(static_cast<ExyokiOffice::Int64>(1));
        hosted.element->SetYCoordinate(sampleYCoordinate);
        CHECK(hosted.element->GetYCoordinate() == sampleYCoordinate);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetTime() == sampleTime);
        CHECK(reloaded.element->GetXCoordinate() == sampleXCoordinate);
        CHECK(reloaded.element->GetYCoordinate() == sampleYCoordinate);
    }

    TEST_CASE("Section typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::Section>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetName(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetName().IsDefined());
        hosted.element->SetId(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetId().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::StringValue sampleName = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetName(sampleName);
        CHECK(hosted.element->GetName() == sampleName);
        const ExyokiOffice::StringValue sampleId = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetId(sampleId);
        CHECK(hosted.element->GetId() == sampleId);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetName() == sampleName);
        CHECK(reloaded.element->GetId() == sampleId);
    }

    TEST_CASE("SectionSlideIdList typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::SectionSlideIdList>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("SectionSlideIdListEntry typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::SectionSlideIdListEntry>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetId(ExyokiOffice::UInt32Value{});
        CHECK_FALSE(hosted.element->GetId().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::UInt32Value sampleId = ExyokiOffice::UInt32Value(static_cast<ExyokiOffice::UInt32>(1));
        hosted.element->SetId(sampleId);
        CHECK(hosted.element->GetId() == sampleId);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetId() == sampleId);
    }

    TEST_CASE("SectionOld typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::SectionOld>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetName(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetName().IsDefined());
        hosted.element->SetId(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetId().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::StringValue sampleName = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetName(sampleName);
        CHECK(hosted.element->GetName() == sampleName);
        const ExyokiOffice::StringValue sampleId = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetId(sampleId);
        CHECK(hosted.element->GetId() == sampleId);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetName() == sampleName);
        CHECK(reloaded.element->GetId() == sampleId);
    }

    TEST_CASE("ExtensionList typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::ExtensionList>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("MediaBookmarkList typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::MediaBookmarkList>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("MediaFade typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::MediaFade>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetInDuration(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetInDuration().IsDefined());
        hosted.element->SetOutDuration(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetOutDuration().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::StringValue sampleInDuration = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetInDuration(sampleInDuration);
        CHECK(hosted.element->GetInDuration() == sampleInDuration);
        const ExyokiOffice::StringValue sampleOutDuration = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetOutDuration(sampleOutDuration);
        CHECK(hosted.element->GetOutDuration() == sampleOutDuration);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetInDuration() == sampleInDuration);
        CHECK(reloaded.element->GetOutDuration() == sampleOutDuration);
    }

    TEST_CASE("MediaTrim typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::MediaTrim>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetStart(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetStart().IsDefined());
        hosted.element->SetEnd(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetEnd().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::StringValue sampleStart = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetStart(sampleStart);
        CHECK(hosted.element->GetStart() == sampleStart);
        const ExyokiOffice::StringValue sampleEnd = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetEnd(sampleEnd);
        CHECK(hosted.element->GetEnd() == sampleEnd);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetStart() == sampleStart);
        CHECK(reloaded.element->GetEnd() == sampleEnd);
    }

    TEST_CASE("MediaBookmark typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::MediaBookmark>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetName(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetName().IsDefined());
        hosted.element->SetTime(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetTime().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::StringValue sampleName = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetName(sampleName);
        CHECK(hosted.element->GetName() == sampleName);
        const ExyokiOffice::StringValue sampleTime = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetTime(sampleTime);
        CHECK(hosted.element->GetTime() == sampleTime);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetName() == sampleName);
        CHECK(reloaded.element->GetTime() == sampleTime);
    }

    TEST_CASE("ApplicationNonVisualDrawingProperties typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::ApplicationNonVisualDrawingProperties>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetIsPhoto(ExyokiOffice::BooleanValue{});
        CHECK_FALSE(hosted.element->GetIsPhoto().IsDefined());
        hosted.element->SetUserDrawn(ExyokiOffice::BooleanValue{});
        CHECK_FALSE(hosted.element->GetUserDrawn().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::BooleanValue sampleIsPhoto = ExyokiOffice::BooleanValue(true);
        hosted.element->SetIsPhoto(sampleIsPhoto);
        CHECK(hosted.element->GetIsPhoto() == sampleIsPhoto);
        const ExyokiOffice::BooleanValue sampleUserDrawn = ExyokiOffice::BooleanValue(true);
        hosted.element->SetUserDrawn(sampleUserDrawn);
        CHECK(hosted.element->GetUserDrawn() == sampleUserDrawn);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetIsPhoto() == sampleIsPhoto);
        CHECK(reloaded.element->GetUserDrawn() == sampleUserDrawn);
    }

    TEST_CASE("NonVisualInkContentPartProperties typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::NonVisualInkContentPartProperties>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetIsComment(ExyokiOffice::BooleanValue{});
        CHECK_FALSE(hosted.element->GetIsComment().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::BooleanValue sampleIsComment = ExyokiOffice::BooleanValue(true);
        hosted.element->SetIsComment(sampleIsComment);
        CHECK(hosted.element->GetIsComment() == sampleIsComment);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetIsComment() == sampleIsComment);
    }

    TEST_CASE("NonVisualDrawingProperties typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::NonVisualDrawingProperties>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetId(ExyokiOffice::UInt32Value{});
        CHECK_FALSE(hosted.element->GetId().IsDefined());
        hosted.element->SetName(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetName().IsDefined());
        hosted.element->SetDescription(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetDescription().IsDefined());
        hosted.element->SetHidden(ExyokiOffice::BooleanValue{});
        CHECK_FALSE(hosted.element->GetHidden().IsDefined());
        hosted.element->SetTitle(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetTitle().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::UInt32Value sampleId = ExyokiOffice::UInt32Value(static_cast<ExyokiOffice::UInt32>(1));
        hosted.element->SetId(sampleId);
        CHECK(hosted.element->GetId() == sampleId);
        const ExyokiOffice::StringValue sampleName = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetName(sampleName);
        CHECK(hosted.element->GetName() == sampleName);
        const ExyokiOffice::StringValue sampleDescription = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetDescription(sampleDescription);
        CHECK(hosted.element->GetDescription() == sampleDescription);
        const ExyokiOffice::BooleanValue sampleHidden = ExyokiOffice::BooleanValue(true);
        hosted.element->SetHidden(sampleHidden);
        CHECK(hosted.element->GetHidden() == sampleHidden);
        const ExyokiOffice::StringValue sampleTitle = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetTitle(sampleTitle);
        CHECK(hosted.element->GetTitle() == sampleTitle);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetId() == sampleId);
        CHECK(reloaded.element->GetName() == sampleName);
        CHECK(reloaded.element->GetDescription() == sampleDescription);
        CHECK(reloaded.element->GetHidden() == sampleHidden);
        CHECK(reloaded.element->GetTitle() == sampleTitle);
    }

    TEST_CASE("ShowEventRecordList typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::ShowEventRecordList>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("ModificationId typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::ModificationId>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("CreationId typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::CreationId>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("LaserTraceList typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::LaserTraceList>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("ShowMediaControls typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::ShowMediaControls>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetVal(ExyokiOffice::BooleanValue{});
        CHECK_FALSE(hosted.element->GetVal().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::BooleanValue sampleVal = ExyokiOffice::BooleanValue(true);
        hosted.element->SetVal(sampleVal);
        CHECK(hosted.element->GetVal() == sampleVal);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetVal() == sampleVal);
    }

    TEST_CASE("DiscardImageEditData typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::DiscardImageEditData>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetVal(ExyokiOffice::BooleanValue{});
        CHECK_FALSE(hosted.element->GetVal().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::BooleanValue sampleVal = ExyokiOffice::BooleanValue(true);
        hosted.element->SetVal(sampleVal);
        CHECK(hosted.element->GetVal() == sampleVal);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetVal() == sampleVal);
    }

    TEST_CASE("DefaultImageDpi typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::DefaultImageDpi>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetVal(ExyokiOffice::UInt32Value{});
        CHECK_FALSE(hosted.element->GetVal().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::UInt32Value sampleVal = ExyokiOffice::UInt32Value(static_cast<ExyokiOffice::UInt32>(1));
        hosted.element->SetVal(sampleVal);
        CHECK(hosted.element->GetVal() == sampleVal);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetVal() == sampleVal);
    }

    TEST_CASE("LaserColor typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::LaserColor>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("BrowseMode typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::BrowseMode>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetShowStatus(ExyokiOffice::BooleanValue{});
        CHECK_FALSE(hosted.element->GetShowStatus().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::BooleanValue sampleShowStatus = ExyokiOffice::BooleanValue(true);
        hosted.element->SetShowStatus(sampleShowStatus);
        CHECK(hosted.element->GetShowStatus() == sampleShowStatus);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetShowStatus() == sampleShowStatus);
    }

    TEST_CASE("SectionList typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::SectionList>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("SectionProperties typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::SectionProperties>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("BookmarkTarget typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::BookmarkTarget>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetShapeId(ExyokiOffice::UInt32Value{});
        CHECK_FALSE(hosted.element->GetShapeId().IsDefined());
        hosted.element->SetBookmarkName(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetBookmarkName().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::UInt32Value sampleShapeId = ExyokiOffice::UInt32Value(static_cast<ExyokiOffice::UInt32>(1));
        hosted.element->SetShapeId(sampleShapeId);
        CHECK(hosted.element->GetShapeId() == sampleShapeId);
        const ExyokiOffice::StringValue sampleBookmarkName = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetBookmarkName(sampleBookmarkName);
        CHECK(hosted.element->GetBookmarkName() == sampleBookmarkName);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetShapeId() == sampleShapeId);
        CHECK(reloaded.element->GetBookmarkName() == sampleBookmarkName);
    }

    TEST_CASE("WheelReverseTransition typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::WheelReverseTransition>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetSpokes(ExyokiOffice::UInt32Value{});
        CHECK_FALSE(hosted.element->GetSpokes().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::UInt32Value sampleSpokes = ExyokiOffice::UInt32Value(static_cast<ExyokiOffice::UInt32>(1));
        hosted.element->SetSpokes(sampleSpokes);
        CHECK(hosted.element->GetSpokes() == sampleSpokes);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetSpokes() == sampleSpokes);
    }

    TEST_CASE("RevealTransition typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::RevealTransition>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetThroughBlack(ExyokiOffice::BooleanValue{});
        CHECK_FALSE(hosted.element->GetThroughBlack().IsDefined());
        hosted.element->SetDirection(ExyokiOffice::EnumValue<ExyokiOffice::DocumentFormat::OpenXml::Office2010::PowerPoint::TransitionLeftRightDirectionTypeValues>{});
        CHECK_FALSE(hosted.element->GetDirection().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::BooleanValue sampleThroughBlack = ExyokiOffice::BooleanValue(true);
        hosted.element->SetThroughBlack(sampleThroughBlack);
        CHECK(hosted.element->GetThroughBlack() == sampleThroughBlack);
        const ExyokiOffice::EnumValue<ExyokiOffice::DocumentFormat::OpenXml::Office2010::PowerPoint::TransitionLeftRightDirectionTypeValues> sampleDirection = ExyokiOffice::EnumValue<ExyokiOffice::DocumentFormat::OpenXml::Office2010::PowerPoint::TransitionLeftRightDirectionTypeValues>(ExyokiOffice::DocumentFormat::OpenXml::Office2010::PowerPoint::TransitionLeftRightDirectionTypeValues::Left);
        hosted.element->SetDirection(sampleDirection);
        CHECK(hosted.element->GetDirection() == sampleDirection);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetThroughBlack() == sampleThroughBlack);
        CHECK(reloaded.element->GetDirection() == sampleDirection);
    }

    TEST_CASE("ShredTransition typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::ShredTransition>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetPattern(ExyokiOffice::EnumValue<ExyokiOffice::DocumentFormat::OpenXml::Office2010::PowerPoint::TransitionShredPatternValues>{});
        CHECK_FALSE(hosted.element->GetPattern().IsDefined());
        hosted.element->SetDirection(ExyokiOffice::EnumValue<ExyokiOffice::DocumentFormat::OpenXml::Presentation::TransitionInOutDirectionValues>{});
        CHECK_FALSE(hosted.element->GetDirection().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::EnumValue<ExyokiOffice::DocumentFormat::OpenXml::Office2010::PowerPoint::TransitionShredPatternValues> samplePattern = ExyokiOffice::EnumValue<ExyokiOffice::DocumentFormat::OpenXml::Office2010::PowerPoint::TransitionShredPatternValues>(ExyokiOffice::DocumentFormat::OpenXml::Office2010::PowerPoint::TransitionShredPatternValues::Strip);
        hosted.element->SetPattern(samplePattern);
        CHECK(hosted.element->GetPattern() == samplePattern);
        const ExyokiOffice::EnumValue<ExyokiOffice::DocumentFormat::OpenXml::Presentation::TransitionInOutDirectionValues> sampleDirection = ExyokiOffice::EnumValue<ExyokiOffice::DocumentFormat::OpenXml::Presentation::TransitionInOutDirectionValues>(ExyokiOffice::DocumentFormat::OpenXml::Presentation::TransitionInOutDirectionValues::Out);
        hosted.element->SetDirection(sampleDirection);
        CHECK(hosted.element->GetDirection() == sampleDirection);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetPattern() == samplePattern);
        CHECK(reloaded.element->GetDirection() == sampleDirection);
    }

    TEST_CASE("FlythroughTransition typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::FlythroughTransition>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetDirection(ExyokiOffice::EnumValue<ExyokiOffice::DocumentFormat::OpenXml::Presentation::TransitionInOutDirectionValues>{});
        CHECK_FALSE(hosted.element->GetDirection().IsDefined());
        hosted.element->SetHasBounce(ExyokiOffice::BooleanValue{});
        CHECK_FALSE(hosted.element->GetHasBounce().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::EnumValue<ExyokiOffice::DocumentFormat::OpenXml::Presentation::TransitionInOutDirectionValues> sampleDirection = ExyokiOffice::EnumValue<ExyokiOffice::DocumentFormat::OpenXml::Presentation::TransitionInOutDirectionValues>(ExyokiOffice::DocumentFormat::OpenXml::Presentation::TransitionInOutDirectionValues::Out);
        hosted.element->SetDirection(sampleDirection);
        CHECK(hosted.element->GetDirection() == sampleDirection);
        const ExyokiOffice::BooleanValue sampleHasBounce = ExyokiOffice::BooleanValue(true);
        hosted.element->SetHasBounce(sampleHasBounce);
        CHECK(hosted.element->GetHasBounce() == sampleHasBounce);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetDirection() == sampleDirection);
        CHECK(reloaded.element->GetHasBounce() == sampleHasBounce);
    }

    TEST_CASE("WarpTransition typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::WarpTransition>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetDirection(ExyokiOffice::EnumValue<ExyokiOffice::DocumentFormat::OpenXml::Presentation::TransitionInOutDirectionValues>{});
        CHECK_FALSE(hosted.element->GetDirection().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::EnumValue<ExyokiOffice::DocumentFormat::OpenXml::Presentation::TransitionInOutDirectionValues> sampleDirection = ExyokiOffice::EnumValue<ExyokiOffice::DocumentFormat::OpenXml::Presentation::TransitionInOutDirectionValues>(ExyokiOffice::DocumentFormat::OpenXml::Presentation::TransitionInOutDirectionValues::Out);
        hosted.element->SetDirection(sampleDirection);
        CHECK(hosted.element->GetDirection() == sampleDirection);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetDirection() == sampleDirection);
    }

    TEST_CASE("GlitterTransition typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::GlitterTransition>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetDirection(ExyokiOffice::EnumValue<ExyokiOffice::DocumentFormat::OpenXml::Presentation::TransitionSlideDirectionValues>{});
        CHECK_FALSE(hosted.element->GetDirection().IsDefined());
        hosted.element->SetPattern(ExyokiOffice::EnumValue<ExyokiOffice::DocumentFormat::OpenXml::Office2010::PowerPoint::TransitionPatternValues>{});
        CHECK_FALSE(hosted.element->GetPattern().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::EnumValue<ExyokiOffice::DocumentFormat::OpenXml::Presentation::TransitionSlideDirectionValues> sampleDirection = ExyokiOffice::EnumValue<ExyokiOffice::DocumentFormat::OpenXml::Presentation::TransitionSlideDirectionValues>(ExyokiOffice::DocumentFormat::OpenXml::Presentation::TransitionSlideDirectionValues::Left);
        hosted.element->SetDirection(sampleDirection);
        CHECK(hosted.element->GetDirection() == sampleDirection);
        const ExyokiOffice::EnumValue<ExyokiOffice::DocumentFormat::OpenXml::Office2010::PowerPoint::TransitionPatternValues> samplePattern = ExyokiOffice::EnumValue<ExyokiOffice::DocumentFormat::OpenXml::Office2010::PowerPoint::TransitionPatternValues>(ExyokiOffice::DocumentFormat::OpenXml::Office2010::PowerPoint::TransitionPatternValues::Diamond);
        hosted.element->SetPattern(samplePattern);
        CHECK(hosted.element->GetPattern() == samplePattern);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetDirection() == sampleDirection);
        CHECK(reloaded.element->GetPattern() == samplePattern);
    }

    TEST_CASE("WindowTransition typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::WindowTransition>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("DoorsTransition typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::DoorsTransition>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("PrismTransition typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::PrismTransition>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetDirection(ExyokiOffice::EnumValue<ExyokiOffice::DocumentFormat::OpenXml::Presentation::TransitionSlideDirectionValues>{});
        CHECK_FALSE(hosted.element->GetDirection().IsDefined());
        hosted.element->SetIsContent(ExyokiOffice::BooleanValue{});
        CHECK_FALSE(hosted.element->GetIsContent().IsDefined());
        hosted.element->SetIsInverted(ExyokiOffice::BooleanValue{});
        CHECK_FALSE(hosted.element->GetIsInverted().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::EnumValue<ExyokiOffice::DocumentFormat::OpenXml::Presentation::TransitionSlideDirectionValues> sampleDirection = ExyokiOffice::EnumValue<ExyokiOffice::DocumentFormat::OpenXml::Presentation::TransitionSlideDirectionValues>(ExyokiOffice::DocumentFormat::OpenXml::Presentation::TransitionSlideDirectionValues::Left);
        hosted.element->SetDirection(sampleDirection);
        CHECK(hosted.element->GetDirection() == sampleDirection);
        const ExyokiOffice::BooleanValue sampleIsContent = ExyokiOffice::BooleanValue(true);
        hosted.element->SetIsContent(sampleIsContent);
        CHECK(hosted.element->GetIsContent() == sampleIsContent);
        const ExyokiOffice::BooleanValue sampleIsInverted = ExyokiOffice::BooleanValue(true);
        hosted.element->SetIsInverted(sampleIsInverted);
        CHECK(hosted.element->GetIsInverted() == sampleIsInverted);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetDirection() == sampleDirection);
        CHECK(reloaded.element->GetIsContent() == sampleIsContent);
        CHECK(reloaded.element->GetIsInverted() == sampleIsInverted);
    }

    TEST_CASE("FlashTransition typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::FlashTransition>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("HoneycombTransition typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::HoneycombTransition>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("RippleTransition typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::RippleTransition>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetDirection(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetDirection().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::StringValue sampleDirection = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetDirection(sampleDirection);
        CHECK(hosted.element->GetDirection() == sampleDirection);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetDirection() == sampleDirection);
    }

    TEST_CASE("ConveyorTransition typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::ConveyorTransition>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("GalleryTransition typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::GalleryTransition>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("FerrisTransition typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::FerrisTransition>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("FlipTransition typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::FlipTransition>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("SwitchTransition typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::SwitchTransition>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("PanTransition typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::PanTransition>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("VortexTransition typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::VortexTransition>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("Media typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::Media>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetEmbed(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetEmbed().IsDefined());
        hosted.element->SetLink(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetLink().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::StringValue sampleEmbed = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetEmbed(sampleEmbed);
        CHECK(hosted.element->GetEmbed() == sampleEmbed);
        const ExyokiOffice::StringValue sampleLink = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetLink(sampleLink);
        CHECK(hosted.element->GetLink() == sampleLink);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetEmbed() == sampleEmbed);
        CHECK(reloaded.element->GetLink() == sampleLink);
    }

    TEST_CASE("ExtensionListModify typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::ExtensionListModify>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetModify(ExyokiOffice::BooleanValue{});
        CHECK_FALSE(hosted.element->GetModify().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::BooleanValue sampleModify = ExyokiOffice::BooleanValue(true);
        hosted.element->SetModify(sampleModify);
        CHECK(hosted.element->GetModify() == sampleModify);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetModify() == sampleModify);
    }

    TEST_CASE("Transform2D typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::Transform2D>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetRotation(ExyokiOffice::Int32Value{});
        CHECK_FALSE(hosted.element->GetRotation().IsDefined());
        hosted.element->SetHorizontalFlip(ExyokiOffice::BooleanValue{});
        CHECK_FALSE(hosted.element->GetHorizontalFlip().IsDefined());
        hosted.element->SetVerticalFlip(ExyokiOffice::BooleanValue{});
        CHECK_FALSE(hosted.element->GetVerticalFlip().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::Int32Value sampleRotation = ExyokiOffice::Int32Value(static_cast<ExyokiOffice::Int32>(1));
        hosted.element->SetRotation(sampleRotation);
        CHECK(hosted.element->GetRotation() == sampleRotation);
        const ExyokiOffice::BooleanValue sampleHorizontalFlip = ExyokiOffice::BooleanValue(true);
        hosted.element->SetHorizontalFlip(sampleHorizontalFlip);
        CHECK(hosted.element->GetHorizontalFlip() == sampleHorizontalFlip);
        const ExyokiOffice::BooleanValue sampleVerticalFlip = ExyokiOffice::BooleanValue(true);
        hosted.element->SetVerticalFlip(sampleVerticalFlip);
        CHECK(hosted.element->GetVerticalFlip() == sampleVerticalFlip);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetRotation() == sampleRotation);
        CHECK(reloaded.element->GetHorizontalFlip() == sampleHorizontalFlip);
        CHECK(reloaded.element->GetVerticalFlip() == sampleVerticalFlip);
    }

    TEST_CASE("NonVisualContentPartProperties typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::NonVisualContentPartProperties>();
        REQUIRE(hosted.element != nullptr);
    }

}
