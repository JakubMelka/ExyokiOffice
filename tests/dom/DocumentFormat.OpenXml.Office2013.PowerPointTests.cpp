// Copyright (c) 2026 Jakub Melka and Contributors
// SPDX-License-Identifier: MIT
// See LICENSE file in the project root for full license text.
//
// Derived from the Open-XML-SDK metadata under data/, which is
// Copyright (c) .NET Foundation and Contributors and MIT-licensed.
// See data/LICENSE and THIRD-PARTY-LICENSES.md in the project root.
// -----------------------------------------------------------------------------
// <auto-generated>
//     This file was generated by ExyokiOffice OpenXML generator.
//     Manual changes to this file may cause unexpected behaviour.
// </auto-generated>
// -----------------------------------------------------------------------------

#include "doctest.h"

#include "DomTestSupport.hpp"

#include "ExyokiOffice/DOM/DocumentFormat/OpenXml/Office2013/PowerPoint.hpp"
#include "ExyokiOffice/StandardTypes.hpp"

#include <cstdint>
#include <string_view>
#include <vector>

namespace EoNs = ExyokiOffice::DocumentFormat::OpenXml::Office2013::PowerPoint;

TEST_SUITE("DocumentFormat.OpenXml.Office2013.PowerPoint.DomGeneratedTests")
{
    TEST_CASE("ExtendedGuide typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::ExtendedGuide>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetId(ExyokiOffice::UInt32Value{});
        CHECK_FALSE(hosted.element->GetId().IsDefined());
        hosted.element->SetName(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetName().IsDefined());
        hosted.element->SetOrientation(ExyokiOffice::EnumValue<ExyokiOffice::DocumentFormat::OpenXml::Presentation::DirectionValues>{});
        CHECK_FALSE(hosted.element->GetOrientation().IsDefined());
        hosted.element->SetPosition(ExyokiOffice::Int32Value{});
        CHECK_FALSE(hosted.element->GetPosition().IsDefined());
        hosted.element->SetIsUserDrawn(ExyokiOffice::BooleanValue{});
        CHECK_FALSE(hosted.element->GetIsUserDrawn().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::UInt32Value sampleId = ExyokiOffice::UInt32Value(static_cast<ExyokiOffice::UInt32>(1));
        hosted.element->SetId(sampleId);
        CHECK(hosted.element->GetId() == sampleId);
        const ExyokiOffice::StringValue sampleName = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetName(sampleName);
        CHECK(hosted.element->GetName() == sampleName);
        const ExyokiOffice::EnumValue<ExyokiOffice::DocumentFormat::OpenXml::Presentation::DirectionValues> sampleOrientation = ExyokiOffice::EnumValue<ExyokiOffice::DocumentFormat::OpenXml::Presentation::DirectionValues>(ExyokiOffice::DocumentFormat::OpenXml::Presentation::DirectionValues::Horizontal);
        hosted.element->SetOrientation(sampleOrientation);
        CHECK(hosted.element->GetOrientation() == sampleOrientation);
        const ExyokiOffice::Int32Value samplePosition = ExyokiOffice::Int32Value(static_cast<ExyokiOffice::Int32>(1));
        hosted.element->SetPosition(samplePosition);
        CHECK(hosted.element->GetPosition() == samplePosition);
        const ExyokiOffice::BooleanValue sampleIsUserDrawn = ExyokiOffice::BooleanValue(true);
        hosted.element->SetIsUserDrawn(sampleIsUserDrawn);
        CHECK(hosted.element->GetIsUserDrawn() == sampleIsUserDrawn);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetId() == sampleId);
        CHECK(reloaded.element->GetName() == sampleName);
        CHECK(reloaded.element->GetOrientation() == sampleOrientation);
        CHECK(reloaded.element->GetPosition() == samplePosition);
        CHECK(reloaded.element->GetIsUserDrawn() == sampleIsUserDrawn);
    }

    TEST_CASE("ExtensionList typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::ExtensionList>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("ColorType typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::ColorType>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("ParentCommentIdentifier typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::ParentCommentIdentifier>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetAuthorId(ExyokiOffice::UInt32Value{});
        CHECK_FALSE(hosted.element->GetAuthorId().IsDefined());
        hosted.element->SetIndex(ExyokiOffice::UInt32Value{});
        CHECK_FALSE(hosted.element->GetIndex().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::UInt32Value sampleAuthorId = ExyokiOffice::UInt32Value(static_cast<ExyokiOffice::UInt32>(1));
        hosted.element->SetAuthorId(sampleAuthorId);
        CHECK(hosted.element->GetAuthorId() == sampleAuthorId);
        const ExyokiOffice::UInt32Value sampleIndex = ExyokiOffice::UInt32Value(static_cast<ExyokiOffice::UInt32>(1));
        hosted.element->SetIndex(sampleIndex);
        CHECK(hosted.element->GetIndex() == sampleIndex);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetAuthorId() == sampleAuthorId);
        CHECK(reloaded.element->GetIndex() == sampleIndex);
    }

    TEST_CASE("ChartTrackingReferenceBased typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::ChartTrackingReferenceBased>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetVal(ExyokiOffice::BooleanValue{});
        CHECK_FALSE(hosted.element->GetVal().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::BooleanValue sampleVal = ExyokiOffice::BooleanValue(true);
        hosted.element->SetVal(sampleVal);
        CHECK(hosted.element->GetVal() == sampleVal);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetVal() == sampleVal);
    }

    TEST_CASE("NotesGuideList typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::NotesGuideList>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("SlideGuideList typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::SlideGuideList>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("ThreadingInfo typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::ThreadingInfo>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetTimeZoneBias(ExyokiOffice::Int32Value{});
        CHECK_FALSE(hosted.element->GetTimeZoneBias().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::Int32Value sampleTimeZoneBias = ExyokiOffice::Int32Value(static_cast<ExyokiOffice::Int32>(1));
        hosted.element->SetTimeZoneBias(sampleTimeZoneBias);
        CHECK(hosted.element->GetTimeZoneBias() == sampleTimeZoneBias);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetTimeZoneBias() == sampleTimeZoneBias);
    }

    TEST_CASE("PresenceInfo typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::PresenceInfo>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetUserId(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetUserId().IsDefined());
        hosted.element->SetProviderId(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetProviderId().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::StringValue sampleUserId = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetUserId(sampleUserId);
        CHECK(hosted.element->GetUserId() == sampleUserId);
        const ExyokiOffice::StringValue sampleProviderId = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetProviderId(sampleProviderId);
        CHECK(hosted.element->GetProviderId() == sampleProviderId);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetUserId() == sampleUserId);
        CHECK(reloaded.element->GetProviderId() == sampleProviderId);
    }

    TEST_CASE("PresetTransition typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::PresetTransition>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetPreset(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetPreset().IsDefined());
        hosted.element->SetInvX(ExyokiOffice::BooleanValue{});
        CHECK_FALSE(hosted.element->GetInvX().IsDefined());
        hosted.element->SetInvY(ExyokiOffice::BooleanValue{});
        CHECK_FALSE(hosted.element->GetInvY().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::StringValue samplePreset = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetPreset(samplePreset);
        CHECK(hosted.element->GetPreset() == samplePreset);
        const ExyokiOffice::BooleanValue sampleInvX = ExyokiOffice::BooleanValue(true);
        hosted.element->SetInvX(sampleInvX);
        CHECK(hosted.element->GetInvX() == sampleInvX);
        const ExyokiOffice::BooleanValue sampleInvY = ExyokiOffice::BooleanValue(true);
        hosted.element->SetInvY(sampleInvY);
        CHECK(hosted.element->GetInvY() == sampleInvY);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetPreset() == samplePreset);
        CHECK(reloaded.element->GetInvX() == sampleInvX);
        CHECK(reloaded.element->GetInvY() == sampleInvY);
    }

}
