// Copyright (c) 2026 Jakub Melka and Collaborators
// SPDX-License-Identifier: MIT
// See LICENSE file in the project root for full license text.
// -----------------------------------------------------------------------------
// <auto-generated>
//     This file was generated by ExyokiOffice OpenXML generator.
//     Manual changes to this file may cause unexpected behaviour.
// </auto-generated>
// -----------------------------------------------------------------------------

#include "doctest.h"

#include "DomTestSupport.hpp"

#include "ExyokiOffice/DOM/DocumentFormat/OpenXml/Office2021/PowerPoint/Tasks.hpp"
#include "ExyokiOffice/StandardTypes.hpp"

#include <cstdint>
#include <string_view>
#include <vector>

namespace EoNs = ExyokiOffice::DocumentFormat::OpenXml::Office2021::PowerPoint::Tasks;

TEST_SUITE("DocumentFormat.OpenXml.Office2021.PowerPoint.Tasks.DomGeneratedTests")
{
    TEST_CASE("TaskHistory typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::TaskHistory>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("TaskHistoryEvent typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::TaskHistoryEvent>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetTime(ExyokiOffice::DateTimeValue{});
        CHECK_FALSE(hosted.element->GetTime().IsDefined());
        hosted.element->SetId(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetId().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::DateTimeValue sampleTime = ExyokiOffice::DateTimeValue(std::string_view("2020-01-01T00:00:00Z"));
        hosted.element->SetTime(sampleTime);
        CHECK(hosted.element->GetTime() == sampleTime);
        const ExyokiOffice::StringValue sampleId = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetId(sampleId);
        CHECK(hosted.element->GetId() == sampleId);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetTime() == sampleTime);
        CHECK(reloaded.element->GetId() == sampleId);
    }

    TEST_CASE("TaskUnknownRecord typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::TaskUnknownRecord>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("TaskUndo typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::TaskUndo>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetId(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetId().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::StringValue sampleId = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetId(sampleId);
        CHECK(hosted.element->GetId() == sampleId);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetId() == sampleId);
    }

    TEST_CASE("TaskPriorityRecord typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::TaskPriorityRecord>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetVal(ExyokiOffice::Int32Value{});
        CHECK_FALSE(hosted.element->GetVal().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::Int32Value sampleVal = ExyokiOffice::Int32Value(static_cast<ExyokiOffice::Int32>(1));
        hosted.element->SetVal(sampleVal);
        CHECK(hosted.element->GetVal() == sampleVal);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetVal() == sampleVal);
    }

    TEST_CASE("TaskProgressEventInfo typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::TaskProgressEventInfo>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetVal(ExyokiOffice::Int32Value{});
        CHECK_FALSE(hosted.element->GetVal().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::Int32Value sampleVal = ExyokiOffice::Int32Value(static_cast<ExyokiOffice::Int32>(1));
        hosted.element->SetVal(sampleVal);
        CHECK(hosted.element->GetVal() == sampleVal);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetVal() == sampleVal);
    }

    TEST_CASE("TaskScheduleEventInfo typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::TaskScheduleEventInfo>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetStDt(ExyokiOffice::DateTimeValue{});
        CHECK_FALSE(hosted.element->GetStDt().IsDefined());
        hosted.element->SetEndDt(ExyokiOffice::DateTimeValue{});
        CHECK_FALSE(hosted.element->GetEndDt().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::DateTimeValue sampleStDt = ExyokiOffice::DateTimeValue(std::string_view("2020-01-01T00:00:00Z"));
        hosted.element->SetStDt(sampleStDt);
        CHECK(hosted.element->GetStDt() == sampleStDt);
        const ExyokiOffice::DateTimeValue sampleEndDt = ExyokiOffice::DateTimeValue(std::string_view("2020-01-01T00:00:00Z"));
        hosted.element->SetEndDt(sampleEndDt);
        CHECK(hosted.element->GetEndDt() == sampleEndDt);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetStDt() == sampleStDt);
        CHECK(reloaded.element->GetEndDt() == sampleEndDt);
    }

    TEST_CASE("TaskTitleEventInfo typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::TaskTitleEventInfo>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetVal(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetVal().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::StringValue sampleVal = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetVal(sampleVal);
        CHECK(hosted.element->GetVal() == sampleVal);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetVal() == sampleVal);
    }

    TEST_CASE("UnasgnAllEmpty typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::UnasgnAllEmpty>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("AddEmpty typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::AddEmpty>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("TaskAnchor typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::TaskAnchor>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("UnAsgnTaskAssignUnassignUser typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::UnAsgnTaskAssignUnassignUser>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("AsgnTaskAssignUnassignUser typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::AsgnTaskAssignUnassignUser>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("AtrbtnTaskAssignUnassignUser typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::AtrbtnTaskAssignUnassignUser>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("ExtensionList typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::ExtensionList>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("CommentAnchor typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::CommentAnchor>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetId(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetId().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::StringValue sampleId = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetId(sampleId);
        CHECK(hosted.element->GetId() == sampleId);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetId() == sampleId);
    }

    TEST_CASE("TaskHistoryDetails typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::TaskHistoryDetails>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetId(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetId().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::StringValue sampleId = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetId(sampleId);
        CHECK(hosted.element->GetId() == sampleId);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetId() == sampleId);
    }

}
