// Copyright (c) 2026 Jakub Melka and Contributors
// SPDX-License-Identifier: MIT
// See LICENSE file in the project root for full license text.
//
// Derived from the Open-XML-SDK metadata under data/, which is
// Copyright (c) .NET Foundation and Contributors and MIT-licensed.
// See data/LICENSE and THIRD-PARTY-LICENSES.md in the project root.
// -----------------------------------------------------------------------------
// <auto-generated>
//     This file was generated by ExyokiOffice OpenXML generator.
//     Manual changes to this file may cause unexpected behaviour.
// </auto-generated>
// -----------------------------------------------------------------------------

#include "doctest.h"

#include "DomTestSupport.hpp"

#include "ExyokiOffice/DOM/DocumentFormat/OpenXml/Office2019/Drawing/Animation/Model3D.hpp"
#include "ExyokiOffice/StandardTypes.hpp"

#include <cstdint>
#include <string_view>
#include <vector>

namespace EoNs = ExyokiOffice::DocumentFormat::OpenXml::Office2019::Drawing::Animation::Model3D;

TEST_SUITE("DocumentFormat.OpenXml.Office2019.Drawing.Animation.Model3D.DomGeneratedTests")
{
    TEST_CASE("OfficeArtExtensionList typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::OfficeArtExtensionList>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("AnimationProperties typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::AnimationProperties>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetName(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetName().IsDefined());
        hosted.element->SetLength(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetLength().IsDefined());
        hosted.element->SetCount(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetCount().IsDefined());
        hosted.element->SetAuto(ExyokiOffice::BooleanValue{});
        CHECK_FALSE(hosted.element->GetAuto().IsDefined());
        hosted.element->SetOffset(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetOffset().IsDefined());
        hosted.element->SetSt(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetSt().IsDefined());
        hosted.element->SetEnd(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetEnd().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::StringValue sampleName = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetName(sampleName);
        CHECK(hosted.element->GetName() == sampleName);
        const ExyokiOffice::StringValue sampleLength = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetLength(sampleLength);
        CHECK(hosted.element->GetLength() == sampleLength);
        const ExyokiOffice::StringValue sampleCount = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetCount(sampleCount);
        CHECK(hosted.element->GetCount() == sampleCount);
        const ExyokiOffice::BooleanValue sampleAuto = ExyokiOffice::BooleanValue(true);
        hosted.element->SetAuto(sampleAuto);
        CHECK(hosted.element->GetAuto() == sampleAuto);
        const ExyokiOffice::StringValue sampleOffset = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetOffset(sampleOffset);
        CHECK(hosted.element->GetOffset() == sampleOffset);
        const ExyokiOffice::StringValue sampleSt = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetSt(sampleSt);
        CHECK(hosted.element->GetSt() == sampleSt);
        const ExyokiOffice::StringValue sampleEnd = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetEnd(sampleEnd);
        CHECK(hosted.element->GetEnd() == sampleEnd);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetName() == sampleName);
        CHECK(reloaded.element->GetLength() == sampleLength);
        CHECK(reloaded.element->GetCount() == sampleCount);
        CHECK(reloaded.element->GetAuto() == sampleAuto);
        CHECK(reloaded.element->GetOffset() == sampleOffset);
        CHECK(reloaded.element->GetSt() == sampleSt);
        CHECK(reloaded.element->GetEnd() == sampleEnd);
    }

    TEST_CASE("PosterFrame typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::PosterFrame>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetAnimId(ExyokiOffice::UInt32Value{});
        CHECK_FALSE(hosted.element->GetAnimId().IsDefined());
        hosted.element->SetFrame(ExyokiOffice::Int32Value{});
        CHECK_FALSE(hosted.element->GetFrame().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::UInt32Value sampleAnimId = ExyokiOffice::UInt32Value(static_cast<ExyokiOffice::UInt32>(1));
        hosted.element->SetAnimId(sampleAnimId);
        CHECK(hosted.element->GetAnimId() == sampleAnimId);
        const ExyokiOffice::Int32Value sampleFrame = ExyokiOffice::Int32Value(static_cast<ExyokiOffice::Int32>(1));
        hosted.element->SetFrame(sampleFrame);
        CHECK(hosted.element->GetFrame() == sampleFrame);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetAnimId() == sampleAnimId);
        CHECK(reloaded.element->GetFrame() == sampleFrame);
    }

    TEST_CASE("EmbeddedAnimation typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::EmbeddedAnimation>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetAnimId(ExyokiOffice::UInt32Value{});
        CHECK_FALSE(hosted.element->GetAnimId().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::UInt32Value sampleAnimId = ExyokiOffice::UInt32Value(static_cast<ExyokiOffice::UInt32>(1));
        hosted.element->SetAnimId(sampleAnimId);
        CHECK(hosted.element->GetAnimId() == sampleAnimId);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetAnimId() == sampleAnimId);
    }

}
