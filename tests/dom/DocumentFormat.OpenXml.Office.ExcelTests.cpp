// Copyright (c) 2026 Jakub Melka and Collaborators
// SPDX-License-Identifier: MIT
// See LICENSE file in the project root for full license text.
// -----------------------------------------------------------------------------
// <auto-generated>
//     This file was generated by ExyokiOffice OpenXML generator.
//     Manual changes to this file may cause unexpected behaviour.
// </auto-generated>
// -----------------------------------------------------------------------------

#include "doctest.h"

#include "DomTestSupport.hpp"

#include "ExyokiOffice/DOM/DocumentFormat/OpenXml/Office/Excel.hpp"
#include "ExyokiOffice/StandardTypes.hpp"

#include <cstdint>
#include <string_view>
#include <vector>

namespace EoNs = ExyokiOffice::DocumentFormat::OpenXml::Office::Excel;

TEST_SUITE("DocumentFormat.OpenXml.Office.Excel.DomGeneratedTests")
{
    TEST_CASE("ColumnSortMapItem typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::ColumnSortMapItem>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("RowSortMapItem typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::RowSortMapItem>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("ColumnSortMap typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::ColumnSortMap>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetRef(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetRef().IsDefined());
        hosted.element->SetCount(ExyokiOffice::UInt32Value{});
        CHECK_FALSE(hosted.element->GetCount().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::StringValue sampleRef = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetRef(sampleRef);
        CHECK(hosted.element->GetRef() == sampleRef);
        const ExyokiOffice::UInt32Value sampleCount = ExyokiOffice::UInt32Value(static_cast<ExyokiOffice::UInt32>(1));
        hosted.element->SetCount(sampleCount);
        CHECK(hosted.element->GetCount() == sampleCount);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetRef() == sampleRef);
        CHECK(reloaded.element->GetCount() == sampleCount);
    }

    TEST_CASE("RowSortMap typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::RowSortMap>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetRef(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetRef().IsDefined());
        hosted.element->SetCount(ExyokiOffice::UInt32Value{});
        CHECK_FALSE(hosted.element->GetCount().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::StringValue sampleRef = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetRef(sampleRef);
        CHECK(hosted.element->GetRef() == sampleRef);
        const ExyokiOffice::UInt32Value sampleCount = ExyokiOffice::UInt32Value(static_cast<ExyokiOffice::UInt32>(1));
        hosted.element->SetCount(sampleCount);
        CHECK(hosted.element->GetCount() == sampleCount);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetRef() == sampleRef);
        CHECK(reloaded.element->GetCount() == sampleCount);
    }

    TEST_CASE("Formula typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::Formula>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("ReferenceSequence typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::ReferenceSequence>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("WorksheetSortMap typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::WorksheetSortMap>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("Macrosheet typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::Macrosheet>();
        REQUIRE(hosted.element != nullptr);
    }

}
