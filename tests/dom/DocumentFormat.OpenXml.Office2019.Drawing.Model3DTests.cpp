// Copyright (c) 2026 Jakub Melka and Collaborators
// SPDX-License-Identifier: MIT
// See LICENSE file in the project root for full license text.
// -----------------------------------------------------------------------------
// <auto-generated>
//     This file was generated by ExyokiOffice OpenXML generator.
//     Manual changes to this file may cause unexpected behaviour.
// </auto-generated>
// -----------------------------------------------------------------------------

#include "doctest.h"

#include "DomTestSupport.hpp"

#include "ExyokiOffice/DOM/DocumentFormat/OpenXml/Office2019/Drawing/Model3D.hpp"
#include "ExyokiOffice/StandardTypes.hpp"

#include <cstdint>
#include <string_view>
#include <vector>

namespace EoNs = ExyokiOffice::DocumentFormat::OpenXml::Office2019::Drawing::Model3D;

TEST_SUITE("DocumentFormat.OpenXml.Office2019.Drawing.Model3D.DomGeneratedTests")
{
    TEST_CASE("UnknownLight typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::UnknownLight>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("DirectionalLight typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::DirectionalLight>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetEnabled(ExyokiOffice::BooleanValue{});
        CHECK_FALSE(hosted.element->GetEnabled().IsDefined());
        hosted.element->SetAngularRad(ExyokiOffice::Int32Value{});
        CHECK_FALSE(hosted.element->GetAngularRad().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::BooleanValue sampleEnabled = ExyokiOffice::BooleanValue(true);
        hosted.element->SetEnabled(sampleEnabled);
        CHECK(hosted.element->GetEnabled() == sampleEnabled);
        const ExyokiOffice::Int32Value sampleAngularRad = ExyokiOffice::Int32Value(static_cast<ExyokiOffice::Int32>(1));
        hosted.element->SetAngularRad(sampleAngularRad);
        CHECK(hosted.element->GetAngularRad() == sampleAngularRad);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetEnabled() == sampleEnabled);
        CHECK(reloaded.element->GetAngularRad() == sampleAngularRad);
    }

    TEST_CASE("SpotLight typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::SpotLight>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetEnabled(ExyokiOffice::BooleanValue{});
        CHECK_FALSE(hosted.element->GetEnabled().IsDefined());
        hosted.element->SetRad(ExyokiOffice::Int64Value{});
        CHECK_FALSE(hosted.element->GetRad().IsDefined());
        hosted.element->SetSpotAng(ExyokiOffice::Int32Value{});
        CHECK_FALSE(hosted.element->GetSpotAng().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::BooleanValue sampleEnabled = ExyokiOffice::BooleanValue(true);
        hosted.element->SetEnabled(sampleEnabled);
        CHECK(hosted.element->GetEnabled() == sampleEnabled);
        const ExyokiOffice::Int64Value sampleRad = ExyokiOffice::Int64Value(static_cast<ExyokiOffice::Int64>(1));
        hosted.element->SetRad(sampleRad);
        CHECK(hosted.element->GetRad() == sampleRad);
        const ExyokiOffice::Int32Value sampleSpotAng = ExyokiOffice::Int32Value(static_cast<ExyokiOffice::Int32>(1));
        hosted.element->SetSpotAng(sampleSpotAng);
        CHECK(hosted.element->GetSpotAng() == sampleSpotAng);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetEnabled() == sampleEnabled);
        CHECK(reloaded.element->GetRad() == sampleRad);
        CHECK(reloaded.element->GetSpotAng() == sampleSpotAng);
    }

    TEST_CASE("PointLight typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::PointLight>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetEnabled(ExyokiOffice::BooleanValue{});
        CHECK_FALSE(hosted.element->GetEnabled().IsDefined());
        hosted.element->SetRad(ExyokiOffice::Int64Value{});
        CHECK_FALSE(hosted.element->GetRad().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::BooleanValue sampleEnabled = ExyokiOffice::BooleanValue(true);
        hosted.element->SetEnabled(sampleEnabled);
        CHECK(hosted.element->GetEnabled() == sampleEnabled);
        const ExyokiOffice::Int64Value sampleRad = ExyokiOffice::Int64Value(static_cast<ExyokiOffice::Int64>(1));
        hosted.element->SetRad(sampleRad);
        CHECK(hosted.element->GetRad() == sampleRad);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetEnabled() == sampleEnabled);
        CHECK(reloaded.element->GetRad() == sampleRad);
    }

    TEST_CASE("AmbientLight typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::AmbientLight>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetEnabled(ExyokiOffice::BooleanValue{});
        CHECK_FALSE(hosted.element->GetEnabled().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::BooleanValue sampleEnabled = ExyokiOffice::BooleanValue(true);
        hosted.element->SetEnabled(sampleEnabled);
        CHECK(hosted.element->GetEnabled() == sampleEnabled);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetEnabled() == sampleEnabled);
    }

    TEST_CASE("WindowViewport typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::WindowViewport>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("ObjectViewport typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::ObjectViewport>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetViewportSz(ExyokiOffice::Int64Value{});
        CHECK_FALSE(hosted.element->GetViewportSz().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::Int64Value sampleViewportSz = ExyokiOffice::Int64Value(static_cast<ExyokiOffice::Int64>(1));
        hosted.element->SetViewportSz(sampleViewportSz);
        CHECK(hosted.element->GetViewportSz() == sampleViewportSz);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetViewportSz() == sampleViewportSz);
    }

    TEST_CASE("Model3DExtensionList typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::Model3DExtensionList>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("Model3DRaster typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::Model3DRaster>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetRName(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetRName().IsDefined());
        hosted.element->SetRVer(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetRVer().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::StringValue sampleRName = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetRName(sampleRName);
        CHECK(hosted.element->GetRName() == sampleRName);
        const ExyokiOffice::StringValue sampleRVer = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetRVer(sampleRVer);
        CHECK(hosted.element->GetRVer() == sampleRVer);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetRName() == sampleRName);
        CHECK(reloaded.element->GetRVer() == sampleRVer);
    }

    TEST_CASE("PictureAttributionSourceURL typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::PictureAttributionSourceURL>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetId(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetId().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::StringValue sampleId = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetId(sampleId);
        CHECK(hosted.element->GetId() == sampleId);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetId() == sampleId);
    }

    TEST_CASE("Model3DTransform typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::Model3DTransform>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("Model3DCamera typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::Model3DCamera>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("ShapeProperties typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::ShapeProperties>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetBlackWhiteMode(ExyokiOffice::EnumValue<ExyokiOffice::DocumentFormat::OpenXml::Drawing::BlackWhiteModeValues>{});
        CHECK_FALSE(hosted.element->GetBlackWhiteMode().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::EnumValue<ExyokiOffice::DocumentFormat::OpenXml::Drawing::BlackWhiteModeValues> sampleBlackWhiteMode = ExyokiOffice::EnumValue<ExyokiOffice::DocumentFormat::OpenXml::Drawing::BlackWhiteModeValues>(ExyokiOffice::DocumentFormat::OpenXml::Drawing::BlackWhiteModeValues::Color);
        hosted.element->SetBlackWhiteMode(sampleBlackWhiteMode);
        CHECK(hosted.element->GetBlackWhiteMode() == sampleBlackWhiteMode);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetBlackWhiteMode() == sampleBlackWhiteMode);
    }

    TEST_CASE("Model3DExtension typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::Model3DExtension>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetUri(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetUri().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::StringValue sampleUri = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetUri(sampleUri);
        CHECK(hosted.element->GetUri() == sampleUri);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetUri() == sampleUri);
    }

    TEST_CASE("ColorType typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::ColorType>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("Blip typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::Blip>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetEmbed(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetEmbed().IsDefined());
        hosted.element->SetLink(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetLink().IsDefined());
        hosted.element->SetCompressionState(ExyokiOffice::EnumValue<ExyokiOffice::DocumentFormat::OpenXml::Drawing::BlipCompressionValues>{});
        CHECK_FALSE(hosted.element->GetCompressionState().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::StringValue sampleEmbed = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetEmbed(sampleEmbed);
        CHECK(hosted.element->GetEmbed() == sampleEmbed);
        const ExyokiOffice::StringValue sampleLink = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetLink(sampleLink);
        CHECK(hosted.element->GetLink() == sampleLink);
        const ExyokiOffice::EnumValue<ExyokiOffice::DocumentFormat::OpenXml::Drawing::BlipCompressionValues> sampleCompressionState = ExyokiOffice::EnumValue<ExyokiOffice::DocumentFormat::OpenXml::Drawing::BlipCompressionValues>(ExyokiOffice::DocumentFormat::OpenXml::Drawing::BlipCompressionValues::Email);
        hosted.element->SetCompressionState(sampleCompressionState);
        CHECK(hosted.element->GetCompressionState() == sampleCompressionState);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetEmbed() == sampleEmbed);
        CHECK(reloaded.element->GetLink() == sampleLink);
        CHECK(reloaded.element->GetCompressionState() == sampleCompressionState);
    }

    TEST_CASE("PerspectiveProjection typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::PerspectiveProjection>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetFov(ExyokiOffice::Int32Value{});
        CHECK_FALSE(hosted.element->GetFov().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::Int32Value sampleFov = ExyokiOffice::Int32Value(static_cast<ExyokiOffice::Int32>(1));
        hosted.element->SetFov(sampleFov);
        CHECK(hosted.element->GetFov() == sampleFov);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetFov() == sampleFov);
    }

    TEST_CASE("OrthographicProjection typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::OrthographicProjection>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("LookAtPoint3D typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::LookAtPoint3D>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("PosPoint3D typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::PosPoint3D>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("OfficeArtExtensionList typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::OfficeArtExtensionList>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("Rotate3D typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::Rotate3D>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetAx(ExyokiOffice::Int32Value{});
        CHECK_FALSE(hosted.element->GetAx().IsDefined());
        hosted.element->SetAy(ExyokiOffice::Int32Value{});
        CHECK_FALSE(hosted.element->GetAy().IsDefined());
        hosted.element->SetAz(ExyokiOffice::Int32Value{});
        CHECK_FALSE(hosted.element->GetAz().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::Int32Value sampleAx = ExyokiOffice::Int32Value(static_cast<ExyokiOffice::Int32>(1));
        hosted.element->SetAx(sampleAx);
        CHECK(hosted.element->GetAx() == sampleAx);
        const ExyokiOffice::Int32Value sampleAy = ExyokiOffice::Int32Value(static_cast<ExyokiOffice::Int32>(1));
        hosted.element->SetAy(sampleAy);
        CHECK(hosted.element->GetAy() == sampleAy);
        const ExyokiOffice::Int32Value sampleAz = ExyokiOffice::Int32Value(static_cast<ExyokiOffice::Int32>(1));
        hosted.element->SetAz(sampleAz);
        CHECK(hosted.element->GetAz() == sampleAz);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetAx() == sampleAx);
        CHECK(reloaded.element->GetAy() == sampleAy);
        CHECK(reloaded.element->GetAz() == sampleAz);
    }

    TEST_CASE("Scale3D typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::Scale3D>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("UpVector3D typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::UpVector3D>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("PostTransVector3D typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::PostTransVector3D>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("PreTransVector3D typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::PreTransVector3D>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("IntensityPositiveRatio typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::IntensityPositiveRatio>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("IlluminancePositiveRatio typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::IlluminancePositiveRatio>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("SzPositiveRatio typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::SzPositiveRatio>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("MeterPerModelUnitPositiveRatio typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::MeterPerModelUnitPositiveRatio>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("SzRatio typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::SzRatio>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("SyRatio typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::SyRatio>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("SxRatio typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::SxRatio>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("Model3D typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::Model3D>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetEmbed(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetEmbed().IsDefined());
        hosted.element->SetLink(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetLink().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::StringValue sampleEmbed = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetEmbed(sampleEmbed);
        CHECK(hosted.element->GetEmbed() == sampleEmbed);
        const ExyokiOffice::StringValue sampleLink = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetLink(sampleLink);
        CHECK(hosted.element->GetLink() == sampleLink);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetEmbed() == sampleEmbed);
        CHECK(reloaded.element->GetLink() == sampleLink);
    }

}
