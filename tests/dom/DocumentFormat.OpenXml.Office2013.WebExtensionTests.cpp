// Copyright (c) 2026 Jakub Melka and Contributors
// SPDX-License-Identifier: MIT
// See LICENSE file in the project root for full license text.
//
// Derived from the Open-XML-SDK metadata under data/, which is
// Copyright (c) .NET Foundation and Contributors and MIT-licensed.
// See data/LICENSE and THIRD-PARTY-LICENSES.md in the project root.
// -----------------------------------------------------------------------------
// <auto-generated>
//     This file was generated by ExyokiOffice OpenXML generator.
//     Manual changes to this file may cause unexpected behaviour.
// </auto-generated>
// -----------------------------------------------------------------------------

#include "doctest.h"

#include "DomTestSupport.hpp"

#include "ExyokiOffice/DOM/DocumentFormat/OpenXml/Office2013/WebExtension.hpp"
#include "ExyokiOffice/StandardTypes.hpp"

#include <cstdint>
#include <string_view>
#include <vector>

namespace EoNs = ExyokiOffice::DocumentFormat::OpenXml::Office2013::WebExtension;

TEST_SUITE("DocumentFormat.OpenXml.Office2013.WebExtension.DomGeneratedTests")
{
    TEST_CASE("Snapshot typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::Snapshot>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetEmbed(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetEmbed().IsDefined());
        hosted.element->SetLink(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetLink().IsDefined());
        hosted.element->SetCompressionState(ExyokiOffice::EnumValue<ExyokiOffice::DocumentFormat::OpenXml::Drawing::BlipCompressionValues>{});
        CHECK_FALSE(hosted.element->GetCompressionState().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::StringValue sampleEmbed = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetEmbed(sampleEmbed);
        CHECK(hosted.element->GetEmbed() == sampleEmbed);
        const ExyokiOffice::StringValue sampleLink = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetLink(sampleLink);
        CHECK(hosted.element->GetLink() == sampleLink);
        const ExyokiOffice::EnumValue<ExyokiOffice::DocumentFormat::OpenXml::Drawing::BlipCompressionValues> sampleCompressionState = ExyokiOffice::EnumValue<ExyokiOffice::DocumentFormat::OpenXml::Drawing::BlipCompressionValues>(ExyokiOffice::DocumentFormat::OpenXml::Drawing::BlipCompressionValues::Email);
        hosted.element->SetCompressionState(sampleCompressionState);
        CHECK(hosted.element->GetCompressionState() == sampleCompressionState);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetEmbed() == sampleEmbed);
        CHECK(reloaded.element->GetLink() == sampleLink);
        CHECK(reloaded.element->GetCompressionState() == sampleCompressionState);
    }

    TEST_CASE("WebExtensionBindingList typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::WebExtensionBindingList>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("WebExtensionPropertyBag typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::WebExtensionPropertyBag>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("WebExtensionReferenceList typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::WebExtensionReferenceList>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("WebExtensionStoreReference typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::WebExtensionStoreReference>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetId(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetId().IsDefined());
        hosted.element->SetVersion(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetVersion().IsDefined());
        hosted.element->SetStore(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetStore().IsDefined());
        hosted.element->SetStoreType(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetStoreType().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::StringValue sampleId = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetId(sampleId);
        CHECK(hosted.element->GetId() == sampleId);
        const ExyokiOffice::StringValue sampleVersion = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetVersion(sampleVersion);
        CHECK(hosted.element->GetVersion() == sampleVersion);
        const ExyokiOffice::StringValue sampleStore = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetStore(sampleStore);
        CHECK(hosted.element->GetStore() == sampleStore);
        const ExyokiOffice::StringValue sampleStoreType = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetStoreType(sampleStoreType);
        CHECK(hosted.element->GetStoreType() == sampleStoreType);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetId() == sampleId);
        CHECK(reloaded.element->GetVersion() == sampleVersion);
        CHECK(reloaded.element->GetStore() == sampleStore);
        CHECK(reloaded.element->GetStoreType() == sampleStoreType);
    }

    TEST_CASE("WebExtensionBinding typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::WebExtensionBinding>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetId(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetId().IsDefined());
        hosted.element->SetType(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetType().IsDefined());
        hosted.element->SetAppReference(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetAppReference().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::StringValue sampleId = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetId(sampleId);
        CHECK(hosted.element->GetId() == sampleId);
        const ExyokiOffice::StringValue sampleType = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetType(sampleType);
        CHECK(hosted.element->GetType() == sampleType);
        const ExyokiOffice::StringValue sampleAppReference = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetAppReference(sampleAppReference);
        CHECK(hosted.element->GetAppReference() == sampleAppReference);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetId() == sampleId);
        CHECK(reloaded.element->GetType() == sampleType);
        CHECK(reloaded.element->GetAppReference() == sampleAppReference);
    }

    TEST_CASE("OfficeArtExtensionList typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::OfficeArtExtensionList>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("WebExtensionProperty typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::WebExtensionProperty>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetName(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetName().IsDefined());
        hosted.element->SetValue(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetValue().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::StringValue sampleName = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetName(sampleName);
        CHECK(hosted.element->GetName() == sampleName);
        const ExyokiOffice::StringValue sampleValue = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetValue(sampleValue);
        CHECK(hosted.element->GetValue() == sampleValue);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetName() == sampleName);
        CHECK(reloaded.element->GetValue() == sampleValue);
    }

    TEST_CASE("WebExtensionReference typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::WebExtensionReference>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetId(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetId().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::StringValue sampleId = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetId(sampleId);
        CHECK(hosted.element->GetId() == sampleId);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetId() == sampleId);
    }

    TEST_CASE("WebExtension typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::WebExtension>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetId(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetId().IsDefined());
        hosted.element->SetFrozen(ExyokiOffice::BooleanValue{});
        CHECK_FALSE(hosted.element->GetFrozen().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::StringValue sampleId = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetId(sampleId);
        CHECK(hosted.element->GetId() == sampleId);
        const ExyokiOffice::BooleanValue sampleFrozen = ExyokiOffice::BooleanValue(true);
        hosted.element->SetFrozen(sampleFrozen);
        CHECK(hosted.element->GetFrozen() == sampleFrozen);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetId() == sampleId);
        CHECK(reloaded.element->GetFrozen() == sampleFrozen);
    }

}
