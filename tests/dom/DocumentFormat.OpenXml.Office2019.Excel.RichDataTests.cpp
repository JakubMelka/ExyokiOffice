// Copyright (c) 2026 Jakub Melka and Collaborators
// SPDX-License-Identifier: MIT
// See LICENSE file in the project root for full license text.
// -----------------------------------------------------------------------------
// <auto-generated>
//     This file was generated by ExyokiOffice OpenXML generator.
//     Manual changes to this file may cause unexpected behaviour.
// </auto-generated>
// -----------------------------------------------------------------------------

#include "doctest.h"

#include "DomTestSupport.hpp"

#include "ExyokiOffice/DOM/DocumentFormat/OpenXml/Office2019/Excel/RichData.hpp"
#include "ExyokiOffice/StandardTypes.hpp"

#include <cstdint>
#include <string_view>
#include <vector>

namespace EoNs = ExyokiOffice::DocumentFormat::OpenXml::Office2019::Excel::RichData;

TEST_SUITE("DocumentFormat.OpenXml.Office2019.Excel.RichData.DomGeneratedTests")
{
    TEST_CASE("Key typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::Key>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetN(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetN().IsDefined());
        hosted.element->SetT(ExyokiOffice::EnumValue<ExyokiOffice::DocumentFormat::OpenXml::Office2019::Excel::RichData::RichValueValueType>{});
        CHECK_FALSE(hosted.element->GetT().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::StringValue sampleN = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetN(sampleN);
        CHECK(hosted.element->GetN() == sampleN);
        const ExyokiOffice::EnumValue<ExyokiOffice::DocumentFormat::OpenXml::Office2019::Excel::RichData::RichValueValueType> sampleT = ExyokiOffice::EnumValue<ExyokiOffice::DocumentFormat::OpenXml::Office2019::Excel::RichData::RichValueValueType>(ExyokiOffice::DocumentFormat::OpenXml::Office2019::Excel::RichData::RichValueValueType::d);
        hosted.element->SetT(sampleT);
        CHECK(hosted.element->GetT() == sampleT);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetN() == sampleN);
        CHECK(reloaded.element->GetT() == sampleT);
    }

    TEST_CASE("RichValueStructure typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::RichValueStructure>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetT(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetT().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::StringValue sampleT = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetT(sampleT);
        CHECK(hosted.element->GetT() == sampleT);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetT() == sampleT);
    }

    TEST_CASE("Value typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::Value>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("RichValueFallback typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::RichValueFallback>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetT(ExyokiOffice::EnumValue<ExyokiOffice::DocumentFormat::OpenXml::Office2019::Excel::RichData::RichValueFallbackType>{});
        CHECK_FALSE(hosted.element->GetT().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::EnumValue<ExyokiOffice::DocumentFormat::OpenXml::Office2019::Excel::RichData::RichValueFallbackType> sampleT = ExyokiOffice::EnumValue<ExyokiOffice::DocumentFormat::OpenXml::Office2019::Excel::RichData::RichValueFallbackType>(ExyokiOffice::DocumentFormat::OpenXml::Office2019::Excel::RichData::RichValueFallbackType::b);
        hosted.element->SetT(sampleT);
        CHECK(hosted.element->GetT() == sampleT);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetT() == sampleT);
    }

    TEST_CASE("ExtensionList typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::ExtensionList>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("RichValue typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::RichValue>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetS(ExyokiOffice::UInt32Value{});
        CHECK_FALSE(hosted.element->GetS().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::UInt32Value sampleS = ExyokiOffice::UInt32Value(static_cast<ExyokiOffice::UInt32>(1));
        hosted.element->SetS(sampleS);
        CHECK(hosted.element->GetS() == sampleS);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetS() == sampleS);
    }

    TEST_CASE("RichValueStructures typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::RichValueStructures>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetCount(ExyokiOffice::UInt32Value{});
        CHECK_FALSE(hosted.element->GetCount().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::UInt32Value sampleCount = ExyokiOffice::UInt32Value(static_cast<ExyokiOffice::UInt32>(1));
        hosted.element->SetCount(sampleCount);
        CHECK(hosted.element->GetCount() == sampleCount);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetCount() == sampleCount);
    }

    TEST_CASE("RichValueData typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::RichValueData>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetCount(ExyokiOffice::UInt32Value{});
        CHECK_FALSE(hosted.element->GetCount().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::UInt32Value sampleCount = ExyokiOffice::UInt32Value(static_cast<ExyokiOffice::UInt32>(1));
        hosted.element->SetCount(sampleCount);
        CHECK(hosted.element->GetCount() == sampleCount);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetCount() == sampleCount);
    }

    TEST_CASE("RichValueBlock typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::RichValueBlock>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetI(ExyokiOffice::UInt32Value{});
        CHECK_FALSE(hosted.element->GetI().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::UInt32Value sampleI = ExyokiOffice::UInt32Value(static_cast<ExyokiOffice::UInt32>(1));
        hosted.element->SetI(sampleI);
        CHECK(hosted.element->GetI() == sampleI);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetI() == sampleI);
    }

}
