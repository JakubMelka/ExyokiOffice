// Copyright (c) 2026 Jakub Melka and Contributors
// SPDX-License-Identifier: MIT
// See LICENSE file in the project root for full license text.
//
// Derived from the Open-XML-SDK metadata under data/, which is
// Copyright (c) .NET Foundation and Contributors and MIT-licensed.
// See data/LICENSE and THIRD-PARTY-LICENSES.md in the project root.
// -----------------------------------------------------------------------------
// <auto-generated>
//     This file was generated by ExyokiOffice OpenXML generator.
//     Manual changes to this file may cause unexpected behaviour.
// </auto-generated>
// -----------------------------------------------------------------------------

#include "doctest.h"

#include "DomTestSupport.hpp"

#include "ExyokiOffice/DOM/DocumentFormat/OpenXml/Office/Y2022/FeaturePropertyBag.hpp"
#include "ExyokiOffice/StandardTypes.hpp"

#include <cstdint>
#include <string_view>
#include <vector>

namespace EoNs = ExyokiOffice::DocumentFormat::OpenXml::Office::Y2022::FeaturePropertyBag;

TEST_SUITE("DocumentFormat.OpenXml.Office.Y2022.FeaturePropertyBag.DomGeneratedTests")
{
    TEST_CASE("Xsddouble typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::Xsddouble>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("Xsdboolean typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::Xsdboolean>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("RelXsdstring typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::RelXsdstring>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("SXsdstring typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::SXsdstring>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("Xsdinteger typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::Xsdinteger>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("XsdunsignedInt typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::XsdunsignedInt>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("DifferentialFormatType typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::DifferentialFormatType>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("RelFeatureProperty typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::RelFeatureProperty>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetK(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetK().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::StringValue sampleK = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetK(sampleK);
        CHECK(hosted.element->GetK() == sampleK);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetK() == sampleK);
    }

    TEST_CASE("DecimalFeatureProperty typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::DecimalFeatureProperty>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetK(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetK().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::StringValue sampleK = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetK(sampleK);
        CHECK(hosted.element->GetK() == sampleK);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetK() == sampleK);
    }

    TEST_CASE("BoolFeatureProperty typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::BoolFeatureProperty>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetK(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetK().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::StringValue sampleK = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetK(sampleK);
        CHECK(hosted.element->GetK() == sampleK);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetK() == sampleK);
    }

    TEST_CASE("StringFeatureProperty typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::StringFeatureProperty>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetK(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetK().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::StringValue sampleK = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetK(sampleK);
        CHECK(hosted.element->GetK() == sampleK);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetK() == sampleK);
    }

    TEST_CASE("IntFeatureProperty typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::IntFeatureProperty>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetK(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetK().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::StringValue sampleK = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetK(sampleK);
        CHECK(hosted.element->GetK() == sampleK);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetK() == sampleK);
    }

    TEST_CASE("BagFeatureProperty typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::BagFeatureProperty>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetK(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetK().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::StringValue sampleK = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetK(sampleK);
        CHECK(hosted.element->GetK() == sampleK);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetK() == sampleK);
    }

    TEST_CASE("ArrayFeatureProperty typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::ArrayFeatureProperty>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetK(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetK().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::StringValue sampleK = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetK(sampleK);
        CHECK(hosted.element->GetK() == sampleK);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetK() == sampleK);
    }

    TEST_CASE("ExtensionList typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::ExtensionList>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("FeaturePropertyBag typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::FeaturePropertyBag>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetType(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetType().IsDefined());
        hosted.element->SetExtRef(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetExtRef().IsDefined());
        hosted.element->SetBagExtId(ExyokiOffice::UInt32Value{});
        CHECK_FALSE(hosted.element->GetBagExtId().IsDefined());
        hosted.element->SetAtt(ExyokiOffice::StringValue{});
        CHECK_FALSE(hosted.element->GetAtt().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::StringValue sampleType = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetType(sampleType);
        CHECK(hosted.element->GetType() == sampleType);
        const ExyokiOffice::StringValue sampleExtRef = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetExtRef(sampleExtRef);
        CHECK(hosted.element->GetExtRef() == sampleExtRef);
        const ExyokiOffice::UInt32Value sampleBagExtId = ExyokiOffice::UInt32Value(static_cast<ExyokiOffice::UInt32>(1));
        hosted.element->SetBagExtId(sampleBagExtId);
        CHECK(hosted.element->GetBagExtId() == sampleBagExtId);
        const ExyokiOffice::StringValue sampleAtt = ExyokiOffice::StringValue(std::string_view("eo-sample"));
        hosted.element->SetAtt(sampleAtt);
        CHECK(hosted.element->GetAtt() == sampleAtt);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetType() == sampleType);
        CHECK(reloaded.element->GetExtRef() == sampleExtRef);
        CHECK(reloaded.element->GetBagExtId() == sampleBagExtId);
        CHECK(reloaded.element->GetAtt() == sampleAtt);
    }

    TEST_CASE("BagExtensions typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::BagExtensions>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("ColumnTotalsRevDxfTableRevDxf typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::ColumnTotalsRevDxfTableRevDxf>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("ColumnBodyRevDxfTableRevDxf typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::ColumnBodyRevDxfTableRevDxf>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("ColumnHeaderRevDxfTableRevDxf typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::ColumnHeaderRevDxfTableRevDxf>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("TotalsRowBorderRevDxfTableRevDxf typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::TotalsRowBorderRevDxfTableRevDxf>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("TableBorderRevDxfTableRevDxf typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::TableBorderRevDxfTableRevDxf>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("HeaderRowBorderRevDxfTableRevDxf typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::HeaderRowBorderRevDxfTableRevDxf>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("TotalsRowRevDxfTableRevDxf typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::TotalsRowRevDxfTableRevDxf>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("DataRevDxfTableRevDxf typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::DataRevDxfTableRevDxf>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("HeaderRowRevDxfTableRevDxf typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::HeaderRowRevDxfTableRevDxf>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("RevDxf typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::RevDxf>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("DXFComplement typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::DXFComplement>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetI(ExyokiOffice::UInt32Value{});
        CHECK_FALSE(hosted.element->GetI().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::UInt32Value sampleI = ExyokiOffice::UInt32Value(static_cast<ExyokiOffice::UInt32>(1));
        hosted.element->SetI(sampleI);
        CHECK(hosted.element->GetI() == sampleI);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetI() == sampleI);
    }

    TEST_CASE("XfComplement typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::XfComplement>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetI(ExyokiOffice::UInt32Value{});
        CHECK_FALSE(hosted.element->GetI().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::UInt32Value sampleI = ExyokiOffice::UInt32Value(static_cast<ExyokiOffice::UInt32>(1));
        hosted.element->SetI(sampleI);
        CHECK(hosted.element->GetI() == sampleI);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetI() == sampleI);
    }

    TEST_CASE("FpbsFeaturePropertyBags typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::FpbsFeaturePropertyBags>();
        REQUIRE(hosted.element != nullptr);
    }

    TEST_CASE("FeaturePropertyBags typed accessors round-trip [unit] [metadata] [dom-generated]")
    {
        auto hosted = DomTest::HostElement<EoNs::FeaturePropertyBags>();
        REQUIRE(hosted.element != nullptr);

        // Undefined contract: every typed accessor links and honours IsDefined().
        hosted.element->SetCount(ExyokiOffice::UInt32Value{});
        CHECK_FALSE(hosted.element->GetCount().IsDefined());

        // Typed value round-trip through real XML serialization.
        const ExyokiOffice::UInt32Value sampleCount = ExyokiOffice::UInt32Value(static_cast<ExyokiOffice::UInt32>(1));
        hosted.element->SetCount(sampleCount);
        CHECK(hosted.element->GetCount() == sampleCount);
        const auto reloaded = hosted.RoundTrip();
        REQUIRE(reloaded.element != nullptr);
        CHECK(reloaded.element->GetCount() == sampleCount);
    }

}
